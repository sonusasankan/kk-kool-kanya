import styled from 'styled-components';

const Title = styled.h3`
  color: #292929;
  flex-grow: 1;
  font-size: 1rem;
  font-weight: 500;
  font-family: "TisaOt";
  line-height: 1.25;
  overflow: hidden;

  &.big{
    font-size: 1.5rem;
  }
`;

export default Title;
